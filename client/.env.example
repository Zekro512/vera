# Copy to .env only if the backend is not on http://localhost:5000.
# Local development works without this file at all.
#
# No secret belongs in here. Vite inlines every VITE_ variable into the
# JavaScript bundle it ships to the browser, so anything placed here is public.

VITE_API_URL=http://localhost:5000
